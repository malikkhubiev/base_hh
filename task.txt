1. Убери заглушки in-memory, я сделал Postgres в Docker для отладки, прежде чем уносить на прод.
Прочитать можешь в docker-postgres-db.txt, взаимодействуй с ним.
Сделай код как в db-config.txt, чтобы легко переподключиться к другой бд на проде через env.

2. На Этапе 3, после того как отработал Светофор, выбрали чекбоксами нужных кандидатов и нажали кнопку "Добавить", сервер должен вернуть кандидатов, у каждого из которых:
1) Светофор (без raw и prompt)
2) Залезть на hh, забрать резюме с контактами, а не просто контакты (то есть нужно будет сохранить результаты светофора с предыдущего этапа).

3. Внести изменения в data.txt в соответствии с внесёнными изменениями в проект